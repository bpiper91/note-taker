// GET /api/notes -> read db.json and return all saved notes as json
// POST /api/notes -> receive new note to save in request body, add to db.json, return new note to client (with unique ID)
// DELETE /api/notes/:id -> receive query param of id, read db.json, filter by !id, rewrite db.json

